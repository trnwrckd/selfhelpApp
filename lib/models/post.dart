class Post {
  final String uid;
  String title;
  String content;
  String date;
  String tag;
  //declaring variables

  // constructor
  Post({this.uid, this.title, this.content, this.date, this.tag});
}
